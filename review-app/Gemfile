# frozen_string_literal: true

source "https://rubygems.org"

# Monorepo gems via path
gem "spurline-core", path: ".."
gem "spurline-review", path: "../spurs/spurline-review"
gem "spurline-dashboard", path: "../spurs/spurline-dashboard"

# Web server
gem "puma", "~> 6.0"
gem "sinatra", "~> 4.0"

# Postgres (runtime — required for session persistence)
gem "pg", "~> 1.5"

group :test do
  gem "rspec", "~> 3.12"
  gem "rack-test", "~> 2.1"
  gem "webmock", "~> 3.18"
end
